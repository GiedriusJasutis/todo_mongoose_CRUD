const tasks = require('./../controllers/tasks-ctrl');
const assert = require('chai').assert;

describe('Load index page', function() {
  it('Page loaded succesfully', function(done) {
    //assert.equal(tasks.getIndex());
    done();
  });

  it('Task was added succesfully', function(done) {
    done();
  });
});
